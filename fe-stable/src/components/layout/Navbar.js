import React from "react";

const Navbar = (props) => {
  return (
    <nav className="nav-wrapper deep-purple darken-2 z-depth-3">
      <a href="#" class="brand-logo center">
        RPDS
      </a>
    </nav>
  );
};

export default Navbar;
